describe("installController", function(){
  //Happy Path if installController is given an Array of strings it sorts dependents and installs in order of importance.
  it("It should take an array of strings as packages and return a single string of install order", function(){

  });
});
