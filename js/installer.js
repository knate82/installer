// Create installer that installs packages and all their dependancies.  If a package has dependancy, install dependancy prior to package.

// Accept input from command line
var prompt = require('prompt');
// Start prompt
prompt.start();
